## Prrettier
.prettierrc
```json
{
  "printWidth": 80, // Set the line length to 80 characters
  "tabWidth": 2, // Set the number of spaces per indentation level to 2
  "useTabs": false, // Use spaces instead of tabs for indentation
  "semi": true, // Add semicolons at the end of statements
  "singleQuote": true, // Use single quotes for strings
  "trailingComma": "es5", // Add a trailing comma for arrays and objects (except for functions)
  "bracketSpacing": true, // Add spaces between brackets in object literals
  "arrowParens": "avoid", // Omit parentheses around a single parameter in arrow functions
  "jsxBracketSameLine": false // Put the closing bracket of a JSX element on a new line
}
```